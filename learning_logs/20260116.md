# 입실 체크 해주세요 !! 🎈
## 태그 속성 사용하기
- 태그에 추가 정보를 부여할 때 속성을 사용. 태그 옆에 _특정_ 단어를 붙이고 _값_ 을 지정하면 다양한 표현이 가능.

### 태그의 속성(=특성)
- html 태그의 속성을 영어 원문 상 python과 동일하게 attribute를 사용합니다. 그런데 mdn 문서에서 전에 말했던 것처럼 특성이라고 번역하는 중인데, 현재 국내의 대부분의 강의에서 속성이라고 표기하고 있고 현직에서도 그러는 중이라서 속성이라는 표현을 중심으로 강의합니다.
- 예시
    1. `<html lang="ko">`
    2. `<img src="" alt="">`
이상의 예시에서 img 태그는 그림을 넣어주는 태그인데, 원본 파일명이 src 속성에 들어가고, 만약 그림 파일이 없을 경우 대신 표시할 alt(alternative) 속성이 존재합니다. 근데 이 img 태그 내의 src, alt 속성은 p 태그나 h1 태그에는 존재하지 않습니다. 즉, _태그의 동작을 설정 및 제어하는 것_ 이 **태그의 속성(특성)** 이며, 각 태그마다 설정 가능한 속성들이 있습니다.

`![markdownver](image.png)`
`<img src="./image.png" alt="안나올땐 얘가 출력됨">`

### 인용문 관련 태그
특정 텍스트를 외부에서 가져와 인용하거나 참조할 때 사용하는 태그
1. `<blockquote>` 

```html
<div>
  <blockquote cite="https://www.huxley.net/bnw/four.html">  <!-- cite는 속성 : 출처 표기용 -->
    <p>
      Words can be like X-rays, if you use them properly—they’ll go through
      anything. You read and you’re pierced.
    </p>
  </blockquote>
  <p>—Aldous Huxley, <cite>Brave New World</cite></p>
</div>
```
- ch01_basic_tags 내에 blockquote.html 생성하고, `!+tab` 해서 기초 설정하고, body 태그 내에 이상의 html을 붙여넣은 뒤 live preview를 통해서 우측에 띄우시오. body 태그에 bgcolor="5f5f5f" 하시면 됩니다.

`&lt;blockquote&gt; = <blockquote>`

* 참조
- 어떤 태그를 작성했을 때, img 태그의 경우는 default로 src와 alt 속성이 자동 생성되었지만, 아닌 경우도 있습니다. 그런데 특정 속성이 태그 생성 시에 꼭 포함되었으면 좋겠다면 사용할 수 있는 emmet이 존재합니다. 예를 들어 blockquote를 입력할 때 cite 속성이 같이 자동생성되었으면 좋겠다면
`blockquote[cite` 까지 입력하고 tab 했을 때 완성 됩니다. cite 속성도 만들어집니다.

그리고 less than / greater than 부분 설명하겠습니다.
`<>`를 입력하고자 할 때, .html 파일 내에서 작성하면 tag 이름으로 인식하기 때문에 브라우저에는 출력이 되지 않습니다. 이때 부등호를 다른 대체 텍스트로 입력해주는 작업이 필요한데, 이를 _엔티티 이름_ 이라고 합니다.
* 예시
    1. `<` : &lt; - less than의 의미
    2. `>` : &gt; - greater than의 의미
    3. ` ` : &nbsp; - non-breaking space의 의미

- blockquote 최종 정리
    - 대표적인 특징으로 인용 내용을 들여쓰기로 표현 해줍니다. 근데 이건 적어도 두 세줄 이상일 때 가치가 있겠네요.
    - 이상을 이유로 짧은 텍스트의 경우에는 `<q>`를 사용할 수 있습니다. quote의 축약어겠네요. 마찬가지로 인용이니까 cite 속성을 지니고 있겠습니다.
qtag.html을 만들고 사전 설정

`<mark>` 태그는 단순 강조 뿐만 아니라 _어던 페이지에서 사용자가 특정 키워들르 검색했을 때 해당 키워드를 표시하는 용도_ 로 사용하기도 합니다. 그래서 웹페이지에서 ctrl + f 눌러서 검색했을 때 그 문서 내에 특정 단어가 다 mark 적용되어서 표시됨을 확인할 수 있습니다.

- 머리 글자 태그
```html
<body>
    <h1>abbr 태그 머리 글자 표현하기</h1>
    <p>
        <strong>abbr</strong> 태그를 사용하여
        <abbr title="HyperText Markup Language">HTML</abbr>을 표기한 문단입니다. 페이지의 소스 코드로 코드를 확인해보시면 됩니다.
    </p>
<body>
```

# 태그로 여러 요소 나열하기
- 어떤 정보를 나열할 때 목록으로 표기하면 시인성이 높아집니다. 이런 목록을 표기할 때 순서가 없는 태그와 순서가 존재하는 태그로 나뉩니다.
## 목록을 표현하는 태그
- `<ul>`태그는 unordered list의 축약어로 순서가 상관없는 리스트를 만들 때 사용합니다. 예시 먼저 확인하겠습니다.
```html
<ul>
    <li>사과
    <li>딸기
    <li>포도
</ul>
```
<ul>
    <li>사과
    <li>딸기
    <li>포도
</ul>

- `<ol>` 태그의 경우는 ordered list의 축약어로 순서가 필요한 리스트를 만들 때 사용합니다.
```html
<ol>
    <li>알파
    <li>베타
    <li>감마
</ol>
```
<ol>
    <li>알파
    <li>베타
    <li>감마
</ol>

이상의 두 경우에서 나오는 `<li>` 태그는 list item의 축약어로 `<ul> 및 <ol>`태그의 하위 항목을 표기할 때 사용합니다.
    
특히 `<ol>`의 속성
    1. type = "A"로 지정하면  ABCD...
    2. default 값은 12345 ...
    3. type=을 지정했을 경우 start속성도 지정할 수 있는데 index와 달리 1부터 시작합니다.

## 용어와 정의를 나열하는 태그
어떤 리스트를 1:1로 매칭하는 경우가 있다고 가정하겠습니다. 예를 들어 apple/사과, grape/포도 등입니다. 용어:정의 가 1:1 대응도 가능하고, n:n도 가능할 겁니다. 즉, 여러 용어에 하나의 정의를 붙이거나 하나의 단어에 여러 정의가 있을 수 있습니다.

- `<dl>` 태그 : definition list의 축약어로 정의 목록이라고 번역됩니다.
- `<dt>` 태그 : definition term의 축약어로 정의할 용어를 뜻합니다.
- `<dd>` 태그 : definition description의 축약어로 용어의 설명 혹은 의미 해석으로 볼 수 있습니다.